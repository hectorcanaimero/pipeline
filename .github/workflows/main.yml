name: CI-SFTP
on: push
  branches: [main]
  workflow_dispatch:
  
jobs:
  deploy-via-sftp:
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: wlixcc/SFTP-Deploy-Action@1.2.1
      with:
        server: 173.230.137.242
        username: root
        ssh_private_key: ${{ secrets.ssh_private }}
        password: @@Condor123#@!
        protocol: ftps-legacy
        port: 2299
        local_path: ./
        remote_path: /var/www/html/teste.condor.com.br/